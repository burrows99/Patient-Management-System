# Example environment file for NHS MOA Triage System
# Copy this file to `.env` and fill in values as appropriate.
# Do NOT commit your real `.env`.

# =====================
# Server-side (not exposed to browser)
# =====================
# NHS API key for HRDC Sandbox (get your own if needed)
NHS_API_KEY=SsGQjWX4ka8k1WHXvm5PYlIA6OqXCPcf

# Service ports (containers and local dev)
API_PORT=4001
OAUTH_PORT=3001
CLIENT_PORT=3000

# Optional: explicit issuer for OAuth server (defaults to http://localhost:${OAUTH_PORT})
# OAUTH_ISSUER=

# =====================
# Client-side (exposed to browser via REACT_APP_*)
# =====================
# The browser talks to these URLs; keep them aligned with your compose port mappings
REACT_APP_OAUTH_BASE=http://localhost:3001
REACT_APP_API_BASE=http://localhost:4001
REACT_APP_CLIENT_ID=9c7c344a-51e3-41c0-a655-a3467f2aca57
REACT_APP_REDIRECT_URI=http://localhost:3000/callback
REACT_APP_SCOPE=openid basic_demographics email phone

# =====================
# Dev file watching (optional)
# =====================
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# =====================
# FHIR / HAPI integration
# =====================
# Base URL for the HAPI FHIR server used by api-server and data-loader
HAPI_BASE_URL=http://hapi-server:8080/fhir

# =====================
# Service internal URLs (used between services)
# =====================
# Synthea HTTP interface base (api-server uses this)
SYNTHEA_BASE=http://synthea

# =====================
# Synthea output and API tuning
# =====================
# Directory where Synthea writes JSON outputs (mounted into api-server container)
SYNTHEA_OUTPUT_DIR=/synthea/output

# Optional: control Synthea generate request timeout (ms)
SYNTHEA_GENERATE_TIMEOUT_MS=300000
# Optional: enable extra diagnostics in /synthea/generate
SYNTHEA_DEBUG=false
