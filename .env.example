# Simulation defaults (used as argparse defaults in simulate.py)
SIM_SERVERS=3
# int > 0 (e.g., 1, 2, 4)
SIM_LIMIT=100
# int >= 0 (max encounters), 0 means no explicit cap
SIM_CLASS=
# exact match to ENCOUNTERCLASS in encounters.csv; leave blank for all
# examples: emergency | inpatient | outpatient | urgentcare | ambulatory
SIM_DEBUG=false
# true | false
SIM_POISSON=true
# true | false (enable generated arrival times)
SIM_RATE=0.8
# float > 0 (arrivals per minute), used only when SIM_POISSON=true
SIM_SEED=42
# int (random seed), optional
SIM_START_AT=
# ISO-8601 datetime string; example: 2025-08-24T09:00:00Z

# Ollama settings (preferred over config/ollama_config.yaml)
# Base URL of the Ollama server (Docker default points to the service name)
OLLAMA_HOST=http://ollama:11434
# examples: http://localhost:11434 | http://ollama:11434
# Model to use for Ollama triage
OLLAMA_MODEL=phi:2.7b
# examples: llama3:8b | mistral:7b-instruct | qwen2:7b | phi:2.7b
# Telemetry path for Ollama agent (simulate.py will set a run-scoped default if not present)
OLLAMA_TELEMETRY_PATH=output/telemetry/ollama_telemetry.jsonl
# path to JSONL file; simulate.py auto-sets a run-scoped default if unset
# Optional: force mock behavior for Ollama agent ('cycle','p1','p2','p3','p4','p5')
# OLLAMA_MOCK=cycle  # cycle | p1 | p2 | p3 | p4 | p5
# Optional: explanation detail ('short'|'medium'|'long')
# OLLAMA_EXPLANATION_DETAIL=medium  # short | medium | long

# Other examples
# PATIENT_COUNT=10  # int > 0
